#include<bits/stdc++.h>

using namespace std;

int main(){
    // Pairs;
    // pair<int,string> p;
    // p={1,"one"};
    // p=make_pair(0,"zero");
    // cout<<p.first<<p.second;

    // pair <int,int> p_arr[3];
    // p_arr[0]={1,2};
    // p_arr[1]={3,4};
    // p_arr[2]={5,6};
    // for(int i=0;i<3;i++){
    //     cout<<p_arr[i].first<<" "<<p_arr[i].second<<endl;
    // }

    // Pairs are used to pair two different datatypes;

    // Vectors: vectors are like arrays but having variable size of array;

    // vector<int> v;
    // for(int i=0;i<3;i++){
    //     int x;
    //     cin>>x;
    //     v.push_back(x);  //pushes the the element into the last position
    // }
    
    // for(int i=0;i<v.size();i++){
    //     cout<<v[i]<<" ";
    // }
    // v.pop_back();  // pops back the last element
    // for(int i=0;i<v.size();i++){
    //     cout<<v[i]<<" ";
    // }

    
    return 0;
}